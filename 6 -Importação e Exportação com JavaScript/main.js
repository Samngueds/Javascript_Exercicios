/*
const funçoes = require('./Criando a Função gets e print.js');

console.log(funçoes.print());



const pessoa  = {
    nome: 'mario'
}

const { nome } = pessoa;

const nome = pessoa.nome;

*/
const {gets, print} = require('./Criando a Função gets e print.js');


print (gets());